* namespaces
* interfaces, abstract, final
* trait
* autoloader
* class magic methods
* traversable/array access on objects
* precompilation (put functions and classes at top)
* print
* do while
* (switch)
* alternative syntax if () : endif
* pass, return by reference
* exceptions / errors

* implement standard extensions

* TESTS

